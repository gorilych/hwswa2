description: Checks for WINDNS role, MS DNS Servers
includes: [ wincommon, win.oa.managed.fw, wpe.managed.fw ]
firewall:
  - name: Incoming_from_public
    description: "Allow access to DNS service"
    networks: [frontnet]
    ports: 53
    type: internet
    connect_with:
      hosts: any

