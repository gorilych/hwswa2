description: Checks for PBALINBE role, linux PBA database server
includes: [ common ]
firewall:
  - name: Incoming_from_PBA
    description: Allow access to PostgreSQL from PBA App
    policy: allow
    direction: incoming
    networks: [backnet]
    protos: [TCP, UDP]
    ports: 5432
    type: infra
    connect_with:
      roles: [pbalinfe]

