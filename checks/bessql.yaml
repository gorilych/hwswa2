description: MSSQL server carrying BlackBerry™ Configuration Database.
includes: [ wincommon, ad.member.fw ]
requirements:
  OS_SP: { type: manual, value: 'OS depends if SQL is clustered or not, check manually' }
  arch_os: { type: manual, value: 'Architecture depends on OS, check manually' }
  OSLanguage: en-US
  cpu-cores: 2
  cpu-frequency: 2.3
  ram(GB): 4
  'C:': 40
  'D:': 60
  'Clustered': { type: manual, value: 'Quorum disk and heartbeatNet are needed for clustered configuration, check manually' }
  networks: [backnet]
