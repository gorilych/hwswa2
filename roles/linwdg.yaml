description: Checks for LINWDG role, server for watchdog
includes: [ lincommon, wdg.fw ]
requirements:
  OS: '(CentOS|Red Hat Enterprise Linux Server).* 5\.'
  architecture: x86
  ram(GB): 0.5
  swap(GB): 1
  cpu-frequency: 2
  /: 40
  networks: [backnet, frontnet]
